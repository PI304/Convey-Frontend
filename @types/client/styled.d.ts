type IsHoveredType = {
  isHovered: boolean;
};

type IsFocusedType = {
  isFocused: boolean;
};

type IsCurrentType = {
  isCurrent: boolean;
};

type IsHiddenType = {
  isHidden: boolean;
};

type IsFixedType = {
  isFixed: boolean;
};

type IsSelectedType = {
  isSelected: boolean;
};

type WidthType = {
  width: string;
};

type HeightType = {
  height: string;
};
